/* exported last */

// input: array - Any JavaScript Array
// output: The last element of array.

// return last array index of it's length - 1

function last(array) {
  return array[array.length - 1];
}
