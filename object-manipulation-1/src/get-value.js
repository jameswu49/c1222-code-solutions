/* exported getValue */

// input: object - Any JavaScript Object
// key - A JavaScript String

// output: The value in object found at the key property.

// define function
// return the object parameter with bracket notation of the key parameter

function getValue(object, key) {
  return object[key];
}
