/* exported take */

// input: Array, count

// output: return a new array based on the number of the count parameter

// use and return slice method
//   - start at index 0 and stop at count

function take(array, count) {
  return array.slice(0, count);
}
