/* exported drop */

// input: Array, count

// output: return the elements after the start of the count input

// use and return array using slice method
//   - start at the count with no end

function drop(array, count) {
  return array.slice(count);
}
